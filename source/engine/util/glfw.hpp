// Make sure that glew gets included before glfw
// I have no idea if this even matters but I'm going to do it anyway

#ifndef GLEW_ALREADY_INCL
#define GLEW_ALREADY_INCL
#include <GL/glew.h>
#endif

#include <GLFW/glfw3.h>
